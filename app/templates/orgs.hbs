<h2>My Favorite Orgs</h2>

<ul>
  {{#each this.model as |org|}}
    <li>
      <span {{action 'favoriteClicked' org}}>
        [ Favorite ]
      </span>
      {{link-to org.id "org" org.id}}
    </li>
  {{/each}}
</ul>

{{social-info
  name="John Doe"
  twitter="johndoe"
  github="JohnDoe"
}}

{{social-info
  name="Jane Doe"
  twitter="janedoe"
  github="JaneDoe"
}}

{{outlet}}